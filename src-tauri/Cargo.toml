[package]
name = "cc-session-manager"
version = "0.6.6"
description = "CC Sessions"
authors = ["zjsmew"]
edition = "2021"
default-run = "cc-session-manager"

[lib]
name = "cc_session_manager_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[[bin]]
name = "cc-session-manager"
path = "src/main.rs"
required-features = ["desktop"]

[[bin]]
name = "cc-sessions"
path = "src/bin/cc-sessions.rs"

[features]
default = ["desktop"]
desktop = [
    "dep:tauri",
    "dep:tauri-build",
    "dep:tauri-plugin-clipboard-manager",
    "dep:tauri-plugin-dialog",
    "dep:tauri-plugin-fs",
    "dep:tauri-plugin-single-instance",
    "dep:reqwest",
    "dep:winreg",
]

[build-dependencies]
tauri-build = { version = "2", features = [], optional = true }

[dependencies]
tauri = { version = "2", features = [], optional = true }
tauri-plugin-dialog = { version = "2", optional = true }
tauri-plugin-clipboard-manager = { version = "2", optional = true }
tauri-plugin-fs = { version = "2", optional = true }
tauri-plugin-single-instance = { version = "2.4", optional = true }
reqwest = { version = "0.13", optional = true }
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
rusqlite = { version = "0.32", features = ["bundled"] }
thiserror = "2"
anyhow = "1"
chrono = { version = "0.4", features = ["serde"] }
dirs = "5"
walkdir = "2"
same-file = "1"
sha2 = "0.10"
hex = "0.4"
toml = "0.8"
tiny_http = "0.12"
url = "2"
getrandom = "0.2"
base64 = "0.22"
libc = "0.2"

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61.2", features = ["Win32_Foundation", "Win32_Storage_Packaging_Appx", "Win32_System_Diagnostics_ToolHelp", "Win32_System_Threading"] }
winreg = { version = "0.55", optional = true }

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
